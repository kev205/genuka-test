import { memo } from "react";

function ActivityList() {
  return (
    <div className="flex min-h-screen">
      <h1>This is the list of activities</h1>
    </div>
  );
}

export default memo(ActivityList);
